# Build the Dockerfile for both Node.js and Bun
docker build -f Dockerfile.both -t benchmark-test .

# Run the container
docker run --cpus=6 --memory=12g --rm -v $(pwd):/app benchmark-test bash /app/scripts/run_tests.sh